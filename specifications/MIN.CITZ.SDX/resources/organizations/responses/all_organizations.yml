
description: >-
  The response will be an array of organization objects, each of which will contain the standard organization attributes.

content:
  application/json:
    schema:
      type: array
      items:
        $ref: '../models/organization.yml'
    examples:
      All:
        $ref: 'examples.yml#/organizations_all'

