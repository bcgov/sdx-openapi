extends: [[spectral:oas, off]]  # we turn everything off and only enable what we want

rules:
  # Basic syntactic correctness only
  oas3-schema: warn                     # built-in – validates every schema against JSON Schema
  # oas3-api-servers: error                # must have at least one server URL
  # operation-operationId: warn            # nice to have, but not mandatory yet
  # info-contact: off                      # we don’t enforce contact yet
  # info-license: off
  # path-params: warn                      # warn if path param not used (common source of bugs)

  # # Very light custom branding
  # info-description-contains-company-name:
  #   description: Info description should mention the company or team name
  #   given: $.info.description
  #   severity: warn
  #   then:
  #     function: pattern
  #     functionOptions:
  #       match: (?i)(mycompany|acme|yourteam)